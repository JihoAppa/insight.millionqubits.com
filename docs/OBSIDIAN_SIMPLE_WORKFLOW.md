# Obsidian 간단 워크플로우 - 동기화 버튼 한 번으로 발행

이 가이드는 Obsidian에서 글을 작성하고 폴더 이동 후 **동기화 버튼 한 번만 누르면** 자동으로 블로그에 발행되는 워크플로우를 설명합니다.

## 🎯 목표 워크플로우

```
1. Obsidian에서 글 작성 (초안 폴더)
   ↓
2. 완성된 글을 발행 폴더로 이동
   ↓
3. Obsidian Git 동기화 버튼 클릭 (⚡)
   ↓
4. 자동으로 GitHub에 푸시
   ↓
5. GitHub Actions가 자동 배포
   ↓
6. 5분 후 블로그에 표시
```

---

## 📁 Obsidian Vault 폴더 구조

```
appa-core-vault/
├── 공개저널/
│   ├── .templates/              # 템플릿 폴더
│   │   ├── blog-post-ko.md      # 한국어 포스트 템플릿
│   │   └── blog-post-en.md      # 영어 포스트 템플릿
│   ├── 초안/                    # 작성 중인 글 (블로그에 표시 안 됨)
│   │   ├── draft-post-1.ko.md
│   │   └── draft-post-2.en.md
│   └── 블로그/                  # 발행할 글 (Hugo와 연동)
│       ├── my-post.ko.md        # 발행될 글
│       └── my-post.en.md
└── 비공개/                      # 개인 노트 (블로그 무관)
```

---

## 🔧 초기 설정 (한 번만)

### 1단계: Git 저장소로 Vault 설정

```bash
cd /Users/user/Vaults/appa-core-vault/공개저널
git init
git remote add origin https://github.com/yourusername/paper.millionqubits.com.git
git config user.name "Your Name"
git config user.email "your.email@example.com"
```

### 2단계: 블로그 폴더를 Hugo와 연결

```bash
# 심볼릭 링크 생성
ln -s "/Users/user/Vaults/appa-core-vault/공개저널/블로그" \
      "/Users/user/Websites/paper.millionqubits.com/content/posts"
```

이제 `블로그/` 폴더의 파일이 자동으로 Hugo `content/posts/`에 반영됩니다!

### 3단계: Obsidian Git 플러그인 설치

1. Obsidian 설정 (`Cmd + ,`)
2. Community plugins → Browse
3. "**Obsidian Git**" 검색 및 설치
4. 플러그인 활성화

### 4단계: Obsidian Git 설정

Obsidian 설정 → Obsidian Git:

```yaml
Vault backup interval (minutes): 0        # 자동 커밋 비활성화 (수동으로만)
Auto pull interval (minutes): 10          # 10분마다 원격 변경사항 가져오기
Auto pull on startup: ON                  # 시작 시 자동 pull
Disable push: OFF                         # push 활성화
Push on backup: ON                        # 커밋 시 자동 push
Commit message: "blog: {{date}}"          # 커밋 메시지
```

**핵심**: `Vault backup interval`을 0으로 설정하여 자동 커밋은 비활성화하고, **수동 동기화만** 사용합니다.

### 5단계: 단축키 설정 (선택)

Obsidian 설정 → Hotkeys → "Obsidian Git":

- **Commit and sync (push)**: `Cmd + Shift + S` (또는 원하는 단축키)

---

## ✍️ 일상 워크플로우

### 1. 새 포스트 작성

1. **Templater 또는 Templates로 새 파일 생성**
   - `Cmd + P` → "Templates: Insert template"
   - 템플릿 선택: `blog-post-ko.md` 또는 `blog-post-en.md`

2. **초안 폴더에 저장**
   ```
   공개저널/초안/my-new-post.ko.md
   ```

3. **자유롭게 작성**
   - Front matter에서 `draft: true` 유지
   - 언제든지 저장하고 수정 가능

### 2. 포스트 발행 준비

글이 완성되면:

1. **Front matter 수정**
   ```yaml
   draft: false  # true → false로 변경
   date: 2024-11-24T15:00:00+09:00  # 현재 날짜/시간으로 업데이트
   ```

2. **파일 이동**
   - `초안/my-new-post.ko.md` → `블로그/my-new-post.ko.md`로 드래그 앤 드롭

### 3. 동기화 (발행)

**방법 1: 단축키 사용**
```
Cmd + Shift + S (설정한 단축키)
```

**방법 2: 명령 팔레트**
```
Cmd + P → "Obsidian Git: Commit and sync"
```

**방법 3: 사이드바 버튼**
- 왼쪽 사이드바에 Git 아이콘 (⚡) 클릭

### 4. 확인

- 5-10분 후 https://paper.millionqubits.com 접속
- GitHub Actions 진행 상황: https://github.com/yourusername/paper.millionqubits.com/actions

---

## 📝 템플릿 사용

### Templater 플러그인 설치 (권장)

1. Templater 플러그인 설치
2. 설정:
   ```
   Template folder location: 공개저널/.templates
   Trigger Templater on new file creation: ON
   ```

3. 단축키: `Cmd + T`

### 템플릿 예시

**한국어 템플릿** (`공개저널/.templates/blog-post-ko.md`):

```markdown
---
title: "<% tp.file.title %>"
date: <% tp.date.now("YYYY-MM-DDTHH:mm:ss+09:00") %>
draft: true
tags: []
categories: []
author: "지호아빠"
showToc: true
TocOpen: false
description: ""
---

## 개요

여기에 포스트 내용을 작성하세요.

## 본문

### 섹션 1

내용...

### 섹션 2

내용...

## 마치며

요약 및 마무리...
```

---

## 🖼️ 이미지 처리

### 이미지 첨부 설정

Obsidian 설정 → Files & Links:

```yaml
Default location for new attachments: In subfolder under current folder
Subfolder name: images
```

### 이미지 추가

1. **이미지 붙여넣기 또는 드래그**
   - 자동으로 `images/` 서브폴더에 저장됨

2. **마크다운에서 참조**
   ```markdown
   ![이미지 설명](images/my-image.png)
   ```

3. **글 이동 시 이미지도 함께 이동**
   ```
   초안/my-post.ko.md          →  블로그/my-post.ko.md
   초안/images/my-image.png    →  블로그/images/my-image.png
   ```

---

## 🔄 다국어 포스트

### 한국어/영어 버전 연결

파일명의 기본 부분을 동일하게:

```
블로그/my-post.ko.md    # 한국어
블로그/my-post.en.md    # 영어
```

Hugo가 자동으로 언어 전환 버튼 생성!

### 워크플로우

1. 한국어 버전 작성 및 이동: `블로그/my-post.ko.md`
2. 영어 버전 작성 및 이동: `블로그/my-post.en.md`
3. 동기화 버튼 한 번 클릭
4. 두 버전 모두 자동 발행

---

## ⚠️ 주의사항

### draft 상태 관리

```yaml
# 초안 폴더에 있을 때
draft: true   # 이렇게 유지

# 블로그 폴더로 이동 전에 변경
draft: false  # 이렇게 변경
```

### 날짜 확인

```yaml
# 미래 날짜는 표시 안 됨!
date: 2024-11-24T15:00:00+09:00  # 현재 또는 과거 날짜
```

### 파일명 규칙

```
✅ my-post.ko.md        # 올바름
✅ my-post.en.md        # 올바름
❌ my-post.md           # 언어 지정 필요
❌ my post.ko.md        # 공백 사용 금지 (하이픈 사용)
```

---

## 🐛 문제 해결

### Git 충돌 발생 시

Obsidian Git 플러그인이 자동으로 처리하지만, 수동 해결이 필요한 경우:

```bash
cd /Users/user/Vaults/appa-core-vault/공개저널
git stash
git pull origin main
git stash pop
# 충돌 해결 후
git add .
git commit -m "Resolve conflicts"
git push origin main
```

### 동기화 버튼이 작동하지 않을 때

1. Git 설정 확인:
   ```bash
   cd /Users/user/Vaults/appa-core-vault/공개저널
   git config --list
   ```

2. Obsidian Git 플러그인 로그 확인:
   - 설정 → Obsidian Git → "Show console logs"

3. 수동으로 테스트:
   ```bash
   git status
   git add .
   git commit -m "test"
   git push origin main
   ```

### 포스트가 표시되지 않음

체크리스트:
- [ ] `draft: false`로 설정했나요?
- [ ] 파일이 `블로그/` 폴더에 있나요?
- [ ] 파일명이 `.ko.md` 또는 `.en.md`로 끝나나요?
- [ ] 날짜가 미래로 설정되지 않았나요?
- [ ] GitHub Actions가 성공적으로 완료되었나요?

---

## 📊 워크플로우 요약

| 단계 | 작업 | 소요 시간 |
|------|------|-----------|
| 1 | 템플릿으로 새 파일 생성 (`초안/`) | 10초 |
| 2 | 글 작성 | 30-60분 |
| 3 | `draft: false`, 날짜 수정 | 10초 |
| 4 | `초안/` → `블로그/` 이동 | 5초 |
| 5 | 동기화 버튼 클릭 (⚡) | 1초 |
| 6 | 배포 대기 | 5-10분 |

**총 소요 시간**: 글 작성 시간 + 약 30초 + 배포 대기

---

## 🎉 핵심 포인트

✅ **작성은 초안 폴더에서 자유롭게**  
✅ **완성되면 블로그 폴더로 이동**  
✅ **동기화 버튼 한 번만 클릭**  
✅ **자동으로 배포 완료!**

---

**Happy Blogging! 🚀**

협업/연구 문의: jiho_appa@naver.com

